# The deployed dashboard reads the static snapshot in public/data/v1, so there is
# no backend to host, wake, or configure CORS for.
# Regenerate with: python3 -m api.dump_static
VITE_API_STATIC=1
VITE_API_BASE=/data/v1
